# Project Template

This folder is used by `script_project_init.py` as a skeleton for new customer projects.

If content is added here, every new project automatically gets these files.

Currently empty — the script generates the necessary files itself (PROJECT_MAESTRO.md, PROJECT_STATE.md).
